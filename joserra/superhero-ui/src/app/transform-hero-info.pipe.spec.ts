import { TransformHeroInfoPipe } from './transform-hero-info.pipe';

describe('TransformHeroInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformHeroInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
